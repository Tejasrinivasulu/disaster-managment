# Runtime deps for Render Docker image (frontend built in Dockerfile).
# Keep lean for image size. Full local stack: backend/requirements.txt
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pandas==2.2.3
numpy==2.2.1
scikit-learn==1.6.0
joblib==1.4.2
httpx==0.28.1
python-dotenv==1.0.1
aiofiles==24.1.0
reportlab==4.2.5
email-validator==2.2.0
# OR-Tools omitted in slim image (bundle size). Allocator uses greedy fallback.
# Install ortools locally via backend/requirements.txt for exact LP solve.
